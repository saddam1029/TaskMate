
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskMate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TaskMate" parent="Base.Theme.TaskMate" />

    <style name="Theme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Custom DatePicker style -->
    <style name="CustomDatePicker" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="colorAccent">@color/app_color</item>                   <!--header background-->
        <item name="android:windowBackground">@color/white</item>     <!--calendar background-->
        <item name="android:colorControlActivated">@color/app_color</item> <!--selected day-->
        <item name="android:textColorPrimary">@color/app_color</item> <!--days of the month-->
        <item name="android:textColorSecondary">@color/app_color</item>    <!--days of the week-->
    </style>


    <!-- Custom TimePicker style -->
    <style name="CustomTimePicker" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="colorAccent">@color/app_color</item>                   <!--header background-->
        <item name="android:windowBackground">@color/white</item>     <!--calendar background-->
        <item name="android:colorControlActivated">@color/app_color</item> <!--selected day-->
        <item name="android:textColorPrimary">@color/app_color</item> <!--days of the month-->
        <item name="android:textColorSecondary">@color/app_color</item>    <!--days of the week-->
    </style>

    <style name="CustomAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@drawable/custom_dialog_background</item>
        <item name="android:windowMinWidthMajor">50%</item>
    </style>


</resources>
